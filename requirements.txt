python-dotenv>=1.0.0
pydantic>=2.0.0

# API
fastapi
uvicorn
requests
beautifulsoup4
python-multipart

# Content pipeline (scripts/content_pipeline). markdownify converts the scraped
# recommendation HTML to Markdown; the scraper's own get_text() would strip every link,
# image and bold run, which the corpus depends on.
markdownify>=0.13.0
